# 冻结接口文档 - 服务端实现版

> 文档版本：V1.1  
> 更新日期：2026-04-02  
> 状态：已冻结（成员B服务端已实现11个接口）  
> 适用范围：系统验收前不再修改  
> 基础文档：基于《冻结接口文档.md》（原小组文档）

---

## 1. 文档说明

### 1.1 文档定位

本文档是原《冻结接口文档.md》（小组共同制定）的**服务端实现版**，由成员B（服务端负责人）根据实际代码实现进行更新和补充。

**文档关系：**
- **原冻结接口文档**（冻结接口文档.md）：小组共同制定，定义所有计划接口的规范
- **本文档**（冻结接口文档_服务端实现版.md）：记录实际已实现的接口细节

### 1.2 冻结原则

- 本文档中的接口字段在系统验收前不再修改
- 如需修改，需经组长A、成员B、成员C三方确认
- 新增接口需单独文档说明
- 本文档包含**已实现**和**框架完成**的接口

### 1.3 接口实现状态清单（11个）

| 序号 | 接口 | 状态 | 说明 |
|------|------|------|------|
| 1 | GET /health | ✅ 已实现 | 健康检查，裸结构返回 |
| 2 | POST /api/request/create | ✅ 已实现 | 创建充电请求 |
| 3 | GET /api/request/status/{request_id} | ✅ 已实现 | 查询请求状态 |
| 4 | POST /api/request/cancel_queue | ✅ 已实现 | 取消排队 |
| 5 | POST /api/request/confirm_arrival | ✅ 已实现 | 确认到场 |
| 6 | POST /api/request/interrupt_charge | ✅ 已实现 | 中断充电 |
| 7 | POST /api/request/confirm_leave | ✅ 已实现 | 确认挪车 |
| 8 | GET /api/request/result/{request_id} | ✅ 已实现 | 查询详单账单 |
| 9 | POST /api/request/pay | ✅ 已实现 | 支付确认 |
| 10 | GET /api/stations/overview | ✅ 已实现 | 充电桩总览 |
| 11 | POST /api/batch/simulate | 🏗️ 框架完成 | 批量模拟框架，执行引擎待实现 |

**状态说明：**
- ✅ **已实现** - 服务端代码已完成，可正常调用
- 🏗️ **框架完成** - 接口框架完成，核心逻辑待实现

---

## 2. 通用规范

### 2.1 基础URL

```
http://<服务器IP>:8080
```

### 2.2 统一响应格式

**成功响应：**
```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

**错误响应：**
```json
{
  "code": 1001,
  "message": "Invalid parameters",
  "data": {}
}
```

### 2.3 错误码定义（已冻结）

| 错误码 | 含义 | HTTP状态码 | 使用场景 |
|--------|------|------------|----------|
| 0 | 成功 | 200 | 正常返回 |
| 1001 | 请求参数错误 | 400 | 字段缺失、类型错误、非法值 |
| 1002 | 请求不存在 | 400 | request_id找不到 |
| 1003 | 当前状态不允许该操作 | 400 | 状态机非法跳转 |
| 1004 | 过号，无法确认到场 | 400 | 确认时已过号 |
| 1005 | 已取消，无法继续操作 | 400 | 对取消状态的操作 |
| 1006 | 充电桩故障 | 400 | 桩故障时操作 |
| 1007 | 支付失败 | 400 | 余额不足等 |
| 1099 | 服务器内部错误 | 500 | 未捕获异常 |

### 2.4 时间格式

所有时间字段使用 **ISO 8601** 格式：
```
2026-03-31T10:00:00
```

### 2.5 枚举值定义

**charge_mode（充电模式）：**
- `FAST` - 快充
- `SLOW` - 慢充

**request_status（请求状态）：**
- `PENDING` - 待处理（初始状态）
- `WAITING` - 等待中（已入池）
- `CALLED` - 已叫号
- `CONFIRMED` - 已确认到场
- `CHARGING` - 充电中
- `COMPLETED` - 已完成
- `COMPLETED_EARLY` - 提前完成
- `INTERRUPTED` - 已中断
- `CANCELLED` - 已取消
- `NO_SHOW` - 过号
- `FAULT_REQUEUE` - 故障重排
- `WAITING_TO_LEAVE` - 等待挪车

**station_status（充电桩状态）：**
- `IDLE` - 空闲
- `RESERVED` - 已预留
- `CHARGING` - 充电中
- `WAITING_TO_LEAVE` - 等待挪车
- `FAULT` - 故障

---

## 3. 接口清单（已冻结）

### 3.1 健康检查接口

| 接口 | 方法 | 路径 | 状态 |
|------|------|------|------|
| 健康检查 | GET | /health | ✅ 已实现 |

### 3.2 充电请求接口

| 接口 | 方法 | 路径 | 状态 |
|------|------|------|------|
| 提交充电请求 | POST | /api/request/create | ✅ 已实现 |
| 查询请求状态 | GET | /api/request/status/{request_id} | ✅ 已实现 |
| 取消排队 | POST | /api/request/cancel_queue | ✅ 已实现 |
| 确认到场 | POST | /api/request/confirm_arrival | ✅ 已实现 |
| 中断充电 | POST | /api/request/interrupt_charge | ✅ 已实现 |
| 确认挪车 | POST | /api/request/confirm_leave | ✅ 已实现 |
| 获取详单账单 | GET | /api/request/result/{request_id} | ✅ 已实现 |
| 支付确认 | POST | /api/request/pay | ✅ 已实现 |

---

## 4. 接口详细定义

### 4.1 GET /health

**功能：** 服务健康检查

**实现状态：** ✅ 已实现

**请求参数：** 无

**响应示例（裸结构，非统一包装）：**
```json
{
  "status": "ok",
  "timestamp": "2026-03-31T10:00:00"
}
```

**说明：** 此接口使用裸结构返回，不套用 `{code, message, data}` 统一响应格式，用于最基础的连通性检查。

---

### 4.2 POST /api/request/create

**功能：** 提交充电请求

**请求体：**
```json
{
  "request_time": "2026-03-31T10:00:00",  // 必填，ISO 8601格式
  "charge_mode": "FAST",                   // 必填，枚举：FAST/SLOW
  "request_energy": 20.0                   // 必填，正数，单位：kWh
}
```

**成功响应：**
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "request_id": "REQ001",                  // 请求ID，格式：REQ+序号
    "status": "WAITING",                     // 请求状态
    "estimated_wait_seconds": 600,           // 预计等待秒数
    "estimated_start_time": "2026-03-31T10:10:00",  // 预计开始时间
    "estimated_finish_time": "2026-03-31T11:10:00"  // 预计完成时间
  }
}
```

**错误响应：**
- `1001` - 参数错误（字段缺失、非法值）

---

### 4.3 GET /api/request/status/{request_id}

**功能：** 查询请求状态

**路径参数：**
- `request_id` - 请求ID，如：REQ001

**成功响应：**
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "request_id": "REQ001",
    "status": "WAITING",
    "station_id": "FAST_01",                 // 分配的充电桩编号，可能为null
    "estimated_wait_seconds": 300,           // 预计等待秒数
    "last_called_time": "2026-03-31T10:08:00" // 上次叫号时间，可能为null
  }
}
```

**错误响应：**
- `1002` - 请求不存在

---

### 4.4 POST /api/request/cancel_queue

**功能：** 取消排队

**前置条件：** 请求状态为 PENDING/WAITING/CALLED

**请求体：**
```json
{
  "request_id": "REQ001",                  // 必填
  "cancel_time": "2026-03-31T10:05:00"     // 必填，ISO 8601格式
}
```

**成功响应：**
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "request_id": "REQ001",
    "status": "CANCELLED"
  }
}
```

**错误响应：**
- `1001` - 参数错误
- `1002` - 请求不存在
- `1003` - 当前状态不允许取消

---

### 4.5 POST /api/request/confirm_arrival

**功能：** 确认到场

**前置条件：** 请求状态为 CALLED

**请求体：**
```json
{
  "request_id": "REQ001",                  // 必填
  "confirm_time": "2026-03-31T10:09:00"    // 必填，ISO 8601格式
}
```

**成功响应：**
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "request_id": "REQ001",
    "status": "CONFIRMED",
    "station_id": "FAST_01"                  // 分配的充电桩编号
  }
}
```

**错误响应：**
- `1001` - 参数错误
- `1002` - 请求不存在
- `1003` - 请求不在CALLED状态
- `1004` - 已过号（超过confirm_timeout时间）

---

### 4.6 POST /api/request/interrupt_charge

**功能：** 中断充电

**前置条件：** 请求状态为 CHARGING

**请求体：**
```json
{
  "request_id": "REQ001",                  // 必填
  "interrupt_time": "2026-03-31T10:40:00"  // 必填，ISO 8601格式
}
```

**成功响应：**
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "request_id": "REQ001",
    "status": "INTERRUPTED",
    "actual_energy": 12.5,                   // 实际充电电量（kWh）
    "actual_service_seconds": 1800           // 实际服务时长（秒）
  }
}
```

**错误响应：**
- `1001` - 参数错误
- `1002` - 请求不存在
- `1003` - 请求不在CHARGING状态

**实现说明：**
- 实际充电电量根据充电桩功率和充电时长计算
- 释放充电桩，状态变为IDLE
- 触发重调度（TODO：待调度模块接入）

---

### 4.7 POST /api/request/confirm_leave

**功能：** 确认挪车

**前置条件：** 请求状态为 WAITING_TO_LEAVE

**请求体：**
```json
{
  "request_id": "REQ001",                  // 必填
  "leave_time": "2026-03-31T11:15:00"      // 必填，ISO 8601格式
}
```

**成功响应：**
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "request_id": "REQ001",
    "status": "COMPLETED"
  }
}
```

**错误响应：**
- `1001` - 参数错误
- `1002` - 请求不存在
- `1003` - 请求不在WAITING_TO_LEAVE状态

**实现说明：**
- 释放充电桩，状态变为IDLE
- 触发重调度（TODO：待调度模块接入）

---

## 5. 批量模拟接口（框架完成）

### 5.1 POST /api/batch/simulate

**功能：** 批量模拟接口

**实现状态：** 🏗️ 框架完成，执行引擎待实现

**已完成部分：**
- 场景参数解析（`scenario_parser.py`）
- 用户行为解析（`user_behavior_parser.py`）
- 请求参数校验
- 场景适配与初始化

**待实现部分：**
- 时间序列模拟引擎（依赖调度模块）
- 单用户完整流程模拟
- 真实统计指标计算

**请求体：** 详见原《冻结接口文档.md》第6.10节

**响应说明：** 当前返回框架占位数据，真实数据待执行引擎完成后提供

---

## 6. 状态流转图

```
                    +-----------+
                    |  PENDING  |  (初始状态)
                    +-----+-----+
                          |
                          v
                    +-----------+
         +--------->|  WAITING  |<---------+
         |         +-----+-----+          |
         |               |                |
         |    叫号       v   用户取消      |  故障
         |         +-----------+          |
         +---------|   CALLED  |----------+
                   +-----+-----+
                         |
              确认到场   v   超时过号
                   +-----------+
                   | CONFIRMED |
                   +-----+-----+
                         |
              开始充电   v
                   +-----------+
         +-------->|  CHARGING |<---------+
         |         +-----+-----+          |
         |               |                |
    充电完成             v   用户中断      |
         |         +-----------+          |
         +---------| WAITING_TO|----------+
                   |  _LEAVE   |
                   +-----+-----+
                         |
              确认挪车   v
                   +-----------+
                   | COMPLETED |
                   +-----------+

其他终态：
- CANCELLED (取消)
- INTERRUPTED (中断)
- NO_SHOW (过号)
- FAULT_REQUEUE (故障重排)
```

---

## 6. 数据库表结构（已冻结）

### 6.1 charge_request（充电请求表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| request_id | TEXT | 请求ID，唯一，如REQ001 |
| user_id | INTEGER | 用户ID（可选） |
| charge_mode | TEXT | 充电模式：FAST/SLOW |
| request_energy | REAL | 请求电量（kWh） |
| actual_energy | REAL | 实际电量（kWh） |
| battery_limit_energy | REAL | 电池上限（kWh） |
| status | TEXT | 请求状态 |
| submit_time | TIMESTAMP | 提交时间 |
| last_called_at | TIMESTAMP | 上次叫号时间 |
| confirmed_at | TIMESTAMP | 确认到场时间 |
| assigned_station_id | INTEGER | 分配的充电桩ID |
| actual_service_seconds | INTEGER | 实际服务时长（秒） |
| fault_requeue_flag | INTEGER | 故障重排标记 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

### 6.2 charging_station（充电桩表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| station_code | TEXT | 桩编号，如FAST_01 |
| station_type | TEXT | 类型：FAST/SLOW |
| power_kw | REAL | 功率（kW） |
| status | TEXT | 状态：IDLE/RESERVED/CHARGING/WAITING_TO_LEAVE/FAULT |
| current_request_id | INTEGER | 当前请求ID |
| available_time | TIMESTAMP | 可用时间 |

### 6.3 charging_session（充电会话表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| request_id | INTEGER | 请求ID |
| station_id | INTEGER | 充电桩ID |
| start_time | TIMESTAMP | 开始时间 |
| end_time | TIMESTAMP | 结束时间 |
| actual_energy | REAL | 实际电量 |
| status | TEXT | 会话状态 |
| interrupt_reason | TEXT | 中断原因 |
| left_at | TIMESTAMP | 挪车时间 |
| leave_deadline | TIMESTAMP | 挪车截止时间 |

---

## 7. 变更记录

| 日期 | 版本 | 变更内容 | 变更人 |
|------|------|----------|--------|
| 2026-03-31 | V1.0 | 初始冻结版本，包含7个核心接口 | 成员B |
| 2026-03-31 | V1.0 | 实现 /health 接口，统一响应格式 | 成员B |
| 2026-03-31 | V1.0 | 实现 /api/request/create 接口 | 成员B |
| 2026-03-31 | V1.0 | 实现 /api/request/status/{request_id} 接口 | 成员B |
| 2026-03-31 | V1.0 | 实现 /api/request/cancel_queue 接口 | 成员B |
| 2026-03-31 | V1.0 | 实现 /api/request/confirm_arrival 接口 | 成员B |
| 2026-03-31 | V1.0 | 实现 /api/request/interrupt_charge 接口 | 成员B |
| 2026-03-31 | V1.0 | 实现 /api/request/confirm_leave 接口 | 成员B |
| 2026-03-31 | V1.0 | 定义统一错误码（1001-1007, 1099） | 成员B |
| 2026-03-31 | V1.0 | 定义状态枚举值（PENDING, WAITING, CALLED等） | 成员B |
| 2026-03-31 | V1.0 | 设计数据库表结构（8张表） | 成员B |

---

## 8. 确认签名

本文档已冻结，以下人员确认：

- [ ] 组长A（调度算法/整体架构）
- [ ] 成员B（服务端/联合验收）
- [ ] 成员C（前端实现）

确认日期：___________

---

## 附录A：与原版冻结接口文档的差异说明

本文档基于原《冻结接口文档.md》（以下简称"原文档"）进行实现和更新，以下是主要差异：

### A.1 响应格式统一化

| 项目 | 原文档 | 本文档（已实现） | 差异说明 |
|------|--------|------------------|----------|
| /health 响应 | 直接返回 `{"status": "ok"}` | 统一包装为 `{"code": 0, "message": "success", "data": {"status": "ok"}}` | 所有接口统一使用 `code/message/data` 格式 |

**原因：** 为保证所有接口响应格式一致性，健康检查接口也采用统一响应包装。

### A.2 取消排队前置条件扩展

| 项目 | 原文档 | 本文档（已实现） | 差异说明 |
|------|--------|------------------|----------|
| 可取消状态 | `WAITING` 或 `CALLED` | `PENDING`、`WAITING` 或 `CALLED` | 增加了 `PENDING` 状态 |

**原因：** 在简化实现中，请求创建后初始状态为 `PENDING`，允许用户在此状态取消更符合用户体验。

### A.3 错误码补充HTTP状态码

| 项目 | 原文档 | 本文档（已实现） | 差异说明 |
|------|--------|------------------|----------|
| 错误码定义 | 仅定义code值 | 增加HTTP状态码列（200/400/500） | 明确HTTP响应码 |

**实现映射：**
- `code = 0` → HTTP 200
- `1000 <= code < 2000` → HTTP 400（客户端错误）
- `code >= 2000` → HTTP 500（服务器错误）

### A.4 数据库表字段补充

相比原文档，本文档的数据库表增加了以下字段：

**charge_request 表：**
- `battery_limit_energy` - 电池上限电量（kWh）
- `fault_requeue_flag` - 故障重排标记

**charging_session 表：**
- `leave_deadline` - 挪车截止时间（用于计算占位费）

### A.5 新增实现状态标记

本文档为每个接口增加了实现状态标记：
- ✅ 已实现 - 服务端代码已完成并测试通过
- ⏳ 待实现 - 计划在中期验收前完成

### A.6 删减内容说明

以下原文档内容**未包含**在本文档中（将在后续版本补充）：

| 内容 | 原文档章节 | 状态 |
|------|------------|------|
| 批量模拟接口 | 6.10 POST /api/batch/simulate | ⏳ Day 8-9 实现 |

---

## 附录B：服务端实现新增内容

相比原文档，本文档新增了以下内容：

### B.1 接口实现说明

为每个接口增加了服务端实现细节说明：
- 状态流转逻辑
- 数据库操作说明
- 与调度模块的交互点（TODO标记）

### B.2 状态流转图

增加了可视化的状态流转图，展示从 `PENDING` 到各终态的完整流程。

### B.3 变更记录表格

增加了版本变更历史记录，便于追踪文档演进。

### B.4 确认签名区域

增加了三方确认签名区，确保文档冻结得到全体成员认可。

---

**注意：** 本文档为中期验收前的冻结版本，任何修改需经三方确认并更新版本号。
