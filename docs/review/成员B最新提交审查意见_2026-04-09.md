# 成员B最新提交审查意见（2026-04-09）

> 审查对象：`origin/feature/b-backend-integration` 最新提交  
> 审查基线：当前 `develop` 分支中的 `backend/`  
> 审查重点：是否保留并正确接入成员A已落地的调度内核，是否符合现有冻结文档与增量设计要求

---

## 一、他这次实际做了什么

这次提交并不是“在当前 `develop` 基线之上继续完善后端”，而是把成员B自己较早的一版后端实现继续往前推了一版，主要动作包括：

1. 保留了 `/health` 裸结构返回
2. 保留了 `request_time` 使用客户端传入值
3. 将 `test_scenario_adapter.py` 改成了更轻量、偏框架验证的版本
4. 重写了 `backend/README.md` 和 `docs/冻结接口文档_服务端实现版.md`
5. 但同时把 `develop` 中已经接入的调度预测、事件重调度、批量模拟执行内核、联调收口行为回退掉了

一句话概括：

**他修了一部分旧问题，但没有在当前 `develop` 上继续完成工作，而是把已经收好的后端能力回退成了更早的简化版。**

---

## 二、按严重程度排序的问题

### P0-1：回退了成员A已经接入的真实预测能力

当前 `develop` 中，`POST /api/request/create` 已经不再使用接口层临时公式，而是：

1. 写入请求
2. 调用调度模块统一重算
3. 读取该请求的最新预测结果返回

成员B最新分支却把这部分改回成：

- 队列人数 × 300 秒
- 快慢充固定功率估算
- 路由层手写预测

这会直接导致：

1. 成员A已经落地的调度内核失效
2. 创建请求返回的预测结果重新退回占位逻辑
3. 后续成员C联调依赖的真实预测字段失去可信性

结论：

**这是实质性回退，不是“待完善”。**

### P0-2：回退了关键事件后的统一重调度

当前 `develop` 中：

- 取消排队后会触发重调度
- 中断充电后会触发重调度
- 确认挪车后会触发重调度

成员B最新分支把这三处重新改回了 `TODO`。

这会直接导致：

1. 预测结果不会随着关键事件更新
2. 等待时间和推荐桩号会停留在旧状态
3. 系统行为和“事件驱动重调度”的正式文档不一致

结论：

**这不是“还没继续做”，而是把已经接上的能力重新拔掉了。**

### P0-3：回退了批量模拟执行内核，重新变成框架占位

当前 `develop` 中，`POST /api/batch/simulate` 已经接入 `simulate_batch_case(...)`，能够：

1. 接收正式 `scenario + users`
2. 兼容 `UNIFORM_CAPACITY / STATION_SNAPSHOT`
3. 返回真实的 `summary + results`

成员B最新分支却把它改回成：

1. `simulate_single_user()` 占位返回
2. `calculate_summary()` 指标全是 0
3. 文档直接承认“框架完成，待实现”

这会直接导致：

**批量模拟从“最小闭环可用”退回成“仅有接口外壳”。**

### P0-4：回退了 04-02 本地联调收口后的主流程行为

成员A在 04-02 收口中已经明确完成了：

1. `status` 返回联调必需字段
2. 查询状态前的简化自动推进
3. `confirm_arrival` 成功后直接进入 `CHARGING`
4. `interrupt_charge` 改成待离场语义
5. `confirm_leave` 允许 `INTERRUPTED` 结算离场

成员B最新分支把这些关键收口再次改回了更早的版本。

这会直接导致：

1. 前端任务页联调字段不稳定
2. 正常演示链路重新变脆
3. “当前可演示闭环”不再可靠

结论：

**这是对已经验证成功的联调基线的回退。**

### P1-1：文档口径继续夸大完成度

成员B自己的文档一边写：

- “22项已实现 + 2项框架 = 100%”
- “已实现11个接口”

一边又明确承认：

- 真实调度时间计算待成员A
- 批量模拟执行引擎是框架完成
- 结果收集与返回待完善

结论：

**这不是汇报问题，而是会直接误导组内对当前可交付能力的判断。**

### P1-2：仓库整理仍不合格

本次分支相对 `develop`：

1. 新增了 `backend/charging_system.db`
2. 删除了 `backend/requirements.txt`
3. 删除了若干对接说明文档

这会带来：

1. 可复现环境被破坏
2. 新机器无法直接按依赖启动
3. 二进制数据库文件引入协作冲突风险

结论：

**这说明他不是在做可集成的后端收口，而是在推送个人环境状态。**

---

## 三、为了防止他跳过算法内核，成员A已经做了哪些改进

成员A这边已经不是只“给了一个简化算法文件”，而是专门把内核整理成了可扩展但接口稳定的形态，目的就是：

**让成员B现在就能接入，后续算法增强时不用返工接口层。**

已完成的改进包括：

1. 将调度入口稳定为：
   - `predict_request_by_id`
   - `reschedule_waiting_requests`
   - `trigger_reschedule_for_event`
   - `simulate_batch_case`
2. 将评分逻辑和选桩逻辑从主流程中抽离，后续可以只改调度模块内部，不改路由
3. 将部分状态推进逻辑从路由层收回服务层，减少业务层和算法层耦合
4. 明确了“先接入当前稳定内核，后增强内部算法；保持接口一致性，避免成员B返工”的协作原则，并写入：
   - `docs/04-09/成员A可扩展算法内核实现.md`

这意味着：

1. 成员B没有理由继续在接口层手写预测
2. 成员B没有理由把批量模拟改回占位
3. 成员B应该直接依赖当前 `develop` 的调度入口开发

---

## 四、修改意见

### 1. 立即停止以旧分支为基线继续开发

成员B必须切回当前 `develop` 的 `backend/` 作为唯一正确基线，后续修改只允许在此基础上继续补全，不允许再把旧版本接口逻辑搬回来覆盖。

### 2. 恢复调度内核接入，不允许接口层再写临时预测

必须恢复：

1. `create` 走真实预测入口
2. `cancel / interrupt / leave` 走统一重调度入口
3. `batch/simulate` 走 `simulate_batch_case(...)`

这三项不是“建议”，而是继续协作的底线。

### 3. 恢复 04-02 联调收口后的主流程行为

必须恢复：

1. `status` 返回联调必需字段
2. 简化自动推进逻辑
3. `confirm_arrival -> CHARGING`
4. `interrupt -> WAITING_TO_LEAVE`
5. `confirm_leave` 支持 `INTERRUPTED`

否则前端和演示链路都会重新失效。

### 4. 收回“已完成/100%”表述

在完全恢复 `develop` 已有能力前，不允许再使用：

1. “工作已完成”
2. “100%”
3. “已冻结且不再修改”

应改成：

1. 已实现
2. 已恢复到 `develop` 基线
3. 待继续补全

### 5. 立刻清理仓库脏内容

必须立即处理：

1. 删除 `backend/charging_system.db`
2. 恢复 `backend/requirements.txt`
3. 保留必要的实现说明文档，不要删可复现环境信息

---

## 五、最终结论

这次提交不是“在当前项目基线上的继续完成”，而是：

**在修补少量旧问题的同时，回退了成员A已经接入并验证过的关键后端能力。**

最核心的问题不是“他还没做完”，而是：

**他没有沿着当前 `develop` 的正确方向继续开发，而是重新绕开了已经落地的算法内核。**

如果不先纠正这一点，后续无论继续补多少功能，都会继续扩大分工耦合和分支分裂。
