# 本地测试记录（截至 9.1）

## 测试批次

- 日期：2026-05-07
- 分支：feature/c-frontend-testing
- 提交：c67a9e2
- 后端端口：5000
- 前端地址：http://localhost:3000
- 数据库路径：backend/charging_system.db（本地 SQLite）
- 测试人：本地测试人员

## 环境结果

- [x] 健康检查通过
- [x] 前端构建通过
- [x] 后端 unittest 通过
- [x] 后端 pytest 通过
- [x] 数据库已清空并初始化账号

## 失败项

| 编号 | 模块 | 复现步骤 | 实际结果 | 预期结果 | 严重级别 | 处理状态 |
|------|------|----------|----------|----------|----------|----------|
| 暂无 | - | - | - | - | - | - |

## 通过项摘要

- 1.1 启动前检查已验证：本地 SQLite、后端端口、前端地址、健康检查、前端统一走 `/api`、浏览器站点数据可清理。
- 1.2 数据库基线已验证：业务表已清空，充电桩重置为 `RUNNING`，账号已初始化，清库后无活跃请求。
- 1.3 自动化回归已验证：后端 unittest 通过，pytest 通过；前端构建通过。
- 2.1 统一响应格式已抽查：成功响应包含 `code=0`、`message=success`、`data`，业务错误响应仍包含统一字段。
- 2.2 鉴权与角色已验证：未登录跳转登录页，`USER` 和 `ADMIN` 登录后进入各自首页，跨角色访问被拦截或重定向。
- 2.3 浏览器本地存储边界已验证：清理或伪造旧本地数据后，当前请求和账单仍以后端接口为准。
- 3.1 至 3.5 注册与登录已验证：普通用户登录、管理员登录、登录失败、注册成功、注册失败均符合预期。
- 4.1 至 4.4 账户资料与电池容量已验证：资料展示、普通用户改容量、有活跃请求禁止改容量、管理员改容量及事件日志均已覆盖。
- 5.1 至 5.8 提交充电请求已验证：快充、慢充、排队号、请求电量校验、充电模式校验、单用户活跃请求限制、等候区容量、当前模式无可用桩均已覆盖。
- 6.1 至 6.4 当前请求已验证：无活跃请求、有活跃请求、刷新与重新登录、状态流转均已覆盖。
- 7.1 至 7.4 修改请求已验证：等候区修改模式、非等候区禁止修改模式、等候区修改电量、非等候区禁止修改电量均已覆盖。


## 关键验证结果

- `GET /api/health` 返回 `code=0`，`data.status=ok`。
- 后端 unittest 结果：`Ran 44 tests`，`OK (skipped=2)`。
- 后端 pytest 结果：`42 passed, 2 skipped`。
- 数据库清空后 `charge_request=0`。
- 充电桩基线状态：`FAST_01`、`FAST_02`、`FAST_03`、`SLOW_01`、`SLOW_02` 均为 `RUNNING`。
- 快充排队号使用 `F` 前缀，慢充排队号使用 `T` 前缀。
- 等候区满载后继续提交同模式请求失败，错误码为 `1004`。
- 当前模式无可用桩时提交请求失败，错误码为 `1005`。
- 非等候区修改或取消请求失败，错误码为 `1003`。
- 非法请求电量失败，错误码为 `1008`。


## 备注

- 本记录覆盖范围为本地测试清单 1.1 至 7.4。
- 具体清单勾选状态以 `docs/review/本地测试清单.md` 为准；本文件用于记录本批次测试结论。
- 本批次测试遵循“不修改后端代码”的约束。
